{$assertions on}

var
  Arr: array of Int64 = [100..105];
begin
  Assert(Min(2, 4, 6, 8) = 2);
  Assert(Min(40.5, 40.4, 40.3, 40.2) = 40.2);
  Assert(Min(Arr[0], Arr[1], Arr[3], Arr[4], Arr[5]) = 100);

  Writeln('Min :: Passed');
end;